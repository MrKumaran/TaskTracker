package in.project.tasktracker.Enums.Auth;

public enum AuthEnum {
    PASSWORD_UPDATED,
    PASSWORD_MATCH,
    CREDENTIALS_NOT_MATCH,
    USER_NOT_FOUND,
    PASSWORD_NOT_MATCH,
    ERROR_OCCURRED
}
